const INITIAL_STATE = {
  email:'',
  senha: '',
  isValid: false,
}

const user = (store = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return store;
  }
};

export default user;

// Esse reducer será responsável por tratar as informações da pessoa usuária
const STATE_INITIAL = {
  login: {},
  senha: {},
};

function reducerLogin(state = STATE_INITIAL, action) {
  switch (action.type) {
  default:
    return state;
  }
}

export default reducerLogin;

const INITIAL_STATE = {
  total: 0,
  currentCurrency: 'BRL',
};

const wallet = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default wallet;


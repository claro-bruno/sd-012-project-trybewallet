const initialState = {
  email: '',
  password: '',
  login: false,
};

const UserReducer = (state = initialState, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default UserReducer;
